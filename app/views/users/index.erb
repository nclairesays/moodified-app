<h1> All Them Feelers (users)</h1>

<ul>
    <% @users.each do |user| %>
        <li>
            <%= link_to user.name, user %>
            (<%= link_to 'edit', edit_user_path(user) %>)
        </li>
    <% end %>
</ul>


